local mod = get_mod("ControllerHudKeyboardFix")

return {
	name = "Controller HUD M&K Fix",
	description = mod:localize("Fixes an ordering problem with the HUD icons for bombs and potions when using the controller HUD with mouse & keyboard."),
	is_togglable = false,
}
